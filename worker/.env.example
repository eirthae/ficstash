# FicStash worker secrets — copy to .env and fill in. NEVER commit .env.
# These are server-side only. The service_role key must never appear in the app.
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
AO3_USERNAME=
AO3_PASSWORD=
